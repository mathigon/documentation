extends _template
include _docs

block vars
  - var title = 'boost.ts'
  - var subtitle = 'Simplifying browser APIs – from DOM manipulation to web components, event handling, animations, routing, multi-threading and AJAX requests.'

block body

  .x-green
    .hero.slanted
      .container.narrow
        h1
          +icon('brush', 54)
          | boost.ts
        p.subtitle= subtitle
        .btn-row: a.btn.btn-white.m-green(href="https://github.com/mathigon/boost.js")
          +icon('github', 24)
          | View on Github

    .container.api
      .sidebar
        .sidebar-body
      .api-body
        for module in boost.children
          +docs(module)
        +footer()
