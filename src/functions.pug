extends _template

block vars
  - var title = 'Step Functions'
  - var subtitle = 'Every step in a chapter can be linked to custom JavaScript code that sets up interactivity, events or personalisation'

block body

  .x-yellow
    .hero.slanted
      .container.narrow
        h1
          +icon('settings', 54)
          | Step Functions
        p.subtitle= subtitle

    .container.narrow
      h3.text-center COMING SOON!
      +footer()
